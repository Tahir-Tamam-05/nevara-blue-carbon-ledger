# Plan: Integrate New Landing Page File

## Overview
The user has a new landing page file ready and wants to integrate it as the home page without breaking the application's routing flow.

## Current State
- Main landing page: `client/src/pages/new-landing/Landing.tsx` (mapped to `/`)
- New landing file: `client/src/pages/new-landing/newlanding.tsx` (empty, user will add content)
- Router is in `client/src/App.tsx`

## Integration Prompt

Copy and use this prompt with an agent:

```
You are a frontend integration specialist. Your task is to integrate a new landing page component into the existing application.

**Context:**
- The main app routing is in `client/src/App.tsx`
- The current landing page at `/` uses components from `@/components/layout/LandingLayout`
- The current landing component is at `client/src/pages/new-landing/Landing.tsx`
- The new landing file is at `client/src/pages/new-landing/newlanding.tsx`

**Your task:**
1. Read the new landing page content from `client/src/pages/new-landing/newlanding.tsx`
2. Understand its structure and dependencies
3. Copy the content into `client/src/pages/new-landing/Landing.tsx`, ensuring:
   - All imports are corrected to use existing components from `LandingLayout` where applicable
   - The component exports as default
   - The routing at `/` continues to work without changes to App.tsx
   - No other routes are affected
4. Run the build or dev server to verify the integration works
5. Report any errors and fix them

**Important:** Do NOT modify App.tsx routing - the new landing should work at `/` automatically.
```

## Files Safe to Delete (unused)
- `client/src/pages/landing.tsx`
- `client/src/pages/Landing2.tsx`

